{
  "name": "mcp-server",           // Project name
  "version": "1.0.0",              // Version number
  "type": "module",                // Enables ES module syntax (import/export)
  "dependencies": {                // External libraries your code needs
    "express": "^4.19.0"            // Installs Express web framework
  },
  "scripts": {                      // Commands you can run with npm
    "start": "node index.js"        // "npm start" will run index.js
  }
}
